{
  "name": "leonardo_ai_homework",
  "version": "1.0.0",
  "scripts": {
    "fmt": "prettier --write \"**/**/*.ts\"",
    "test": "npx bddgen && cross-env TEST_ENV=production npx playwright test",
    "debug": "npx bddgen && npx playwright test --debug",
    "report": "npx http-server ./cucumber-report -c-1 -a localhost -o index.html"
  },
  "author": "Ryan Rosello",
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.12.0",
    "cross-env": "^7.0.3",
    "playwright-bdd": "^8.1.0",
    "prettier": "^3.4.2",
    "sharp": "^0.33.5"
  }
}
